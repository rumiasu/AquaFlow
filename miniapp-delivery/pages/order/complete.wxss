.container { background: var(--bg-color); min-height: 100vh; padding-bottom: 200rpx; }

.success-header { background: var(--bg-card); margin: var(--spacing-md); border-radius: var(--radius-lg); box-shadow: var(--shadow-sm); padding: var(--spacing-xl) var(--spacing-lg); text-align: center; }
.success-icon { width: 100rpx; height: 100rpx; background: var(--success-color); color: #fff; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 52rpx; font-weight: 700; margin: 0 auto var(--spacing-md); }
.success-title { font-size: var(--font-xxl); font-weight: 700; color: var(--success-color); display: block; margin-bottom: var(--spacing-sm); }
.success-subtitle { font-size: var(--font-md); color: var(--text-secondary); }

.section { background: var(--bg-card); margin: var(--spacing-md); border-radius: var(--radius-lg); box-shadow: var(--shadow-sm); padding: var(--spacing-lg); }
.section-title { font-size: var(--font-md); font-weight: 600; color: var(--text-primary); margin-bottom: var(--spacing-lg); display: block; }
.required { color: #FF3B30; margin-left: 4rpx; font-size: 26rpx; }

.order-info-row { display: flex; padding: 8rpx 0; border-bottom: 2rpx solid #f5f5f5; }
.order-info-row:last-child { border-bottom: none; }
.order-label { width: 80rpx; font-size: 26rpx; color: var(--text-secondary); flex-shrink: 0; }
.order-value { flex: 1; font-size: 26rpx; color: var(--text-primary); word-break: break-all; }

.item-return { padding: 16rpx 0; }
.item-header { margin-bottom: 12rpx; }
.item-name-wrap { display: flex; align-items: center; gap: 12rpx; }
.item-name { font-size: 30rpx; font-weight: 600; color: var(--text-primary); }
.item-spec { font-size: 24rpx; color: var(--text-secondary); background: #f0f4f8; padding: 4rpx 12rpx; border-radius: 8rpx; }

.barrel-row { display: flex; align-items: center; justify-content: space-between; gap: 16rpx; }
.barrel-label { font-size: 28rpx; color: var(--text-primary); }
.num-blue { font-weight: 700; color: #4F8EF7; }

.barrel-stepper { display: flex; align-items: center; gap: 12rpx; }
.step-btn { width: 64rpx; height: 64rpx; border-radius: 50%; background: #f0f4f8; display: flex; align-items: center; justify-content: center; font-size: 36rpx; font-weight: 300; color: var(--text-primary); border: 2rpx solid #e0e0e0; }
.step-btn:active { background: #e0e7ef; }
.step-btn-disabled { opacity: 0.4; }
.step-input { width: 120rpx; height: 64rpx; text-align: center; font-size: 36rpx; font-weight: 700; border: 2rpx solid var(--border-color); border-radius: var(--radius-md); }

.discrepancy-section { margin-top: 16rpx; padding: 16rpx; background: #FFF8E1; border-radius: var(--radius-md); border: 2rpx solid #FFE082; }
.discrepancy-warn { font-size: 26rpx; color: #E65100; font-weight: 600; margin-bottom: 12rpx; }

.reason-list { display: flex; flex-direction: column; gap: 12rpx; margin-bottom: 12rpx; }
.reason-item { display: flex; align-items: center; gap: 12rpx; background: #fff; padding: 12rpx 16rpx; border-radius: var(--radius-md); border: 2rpx solid #FFE082; }
.reason-label { font-size: 26rpx; color: var(--text-primary); flex: 1; }
.reason-qty-wrap { display: flex; align-items: center; gap: 4rpx; }
.reason-qty-label { font-size: 24rpx; color: var(--text-secondary); }
.reason-qty-input { width: 60rpx; height: 48rpx; text-align: center; font-size: 28rpx; font-weight: 600; border: 2rpx solid var(--border-color); border-radius: 8rpx; }
.reason-remove { width: 40rpx; height: 40rpx; display: flex; align-items: center; justify-content: center; color: #FF3B30; font-size: 28rpx; }

.add-reason-btn { display: flex; align-items: center; justify-content: center; gap: 8rpx; padding: 16rpx; border: 2rpx dashed #FFE082; border-radius: var(--radius-md); background: rgba(255, 224, 130, 0.1); }
.add-reason-icon { font-size: 28rpx; color: #E65100; }
.add-reason-text { font-size: 26rpx; color: #E65100; font-weight: 500; }

.reason-hint { font-size: 22rpx; color: var(--text-secondary); margin-top: 8rpx; text-align: center; }

.item-divider { height: 2rpx; background: #f0f0f0; margin: 16rpx 0; }

.empty-hint { text-align: center; padding: 40rpx; color: var(--text-secondary); font-size: 28rpx; }

.first-order-tip { text-align: center; padding: 32rpx 24rpx; background: #E8F5E9; border-radius: var(--radius-md); border: 2rpx solid #C8E6C9; }
.first-order-icon { font-size: 48rpx; display: block; margin-bottom: 12rpx; }
.first-order-text { font-size: 30rpx; font-weight: 600; color: #2E7D32; display: block; margin-bottom: 8rpx; }
.first-order-desc { font-size: 24rpx; color: #558B2F; display: block; line-height: 1.5; }

.section-collect { background: #FFFBF2; border: 2rpx solid rgba(255, 149, 0, 0.2); }
.collect-card { background: #fff; border-radius: 16rpx; padding: 24rpx; margin-bottom: 20rpx; box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.03); }
.collect-row { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8rpx; }
.collect-label { font-size: 26rpx; color: #666666; }
.collect-amount-text { font-size: 30rpx; font-weight: 700; color: #FF7A00; }

.collect-selector { display: grid; grid-template-columns: 1fr 1fr; gap: 16rpx; }
.collect-option { text-align: center; padding: 20rpx 0; border: 2rpx solid var(--border-color); border-radius: var(--radius-md); background: #fff; transition: all .2s; }
.collect-option.active { border-color: #FF9500; background: rgba(255, 149, 0, 0.1); }
.collect-option-name { font-size: 28rpx; font-weight: 600; color: var(--text-primary); display: block; }
.collect-option.active .collect-option-name { color: #CC7700; }
.collect-option-desc { font-size: 20rpx; color: var(--text-secondary); display: block; margin-top: 4rpx; }

.section-tip { display: block; font-size: var(--font-sm); color: var(--text-secondary); margin-bottom: var(--spacing-md); }
.photo-grid { display: flex; gap: var(--spacing-md); flex-wrap: wrap; }
.photo-item { position: relative; width: 200rpx; height: 200rpx; border-radius: var(--radius-md); overflow: hidden; }
.photo-img { width: 100%; height: 100%; }
.photo-remove { position: absolute; top: 0; right: 0; width: 48rpx; height: 48rpx; background: rgba(0,0,0,.55); color: #fff; font-size: 32rpx; line-height: 48rpx; text-align: center; border-radius: 0 0 0 var(--radius-md); }
.photo-add { width: 200rpx; height: 200rpx; border: 2rpx dashed var(--border-color); border-radius: var(--radius-md); display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 8rpx; background: #fafbfc; }
.photo-add-icon { font-size: 48rpx; }
.photo-add-text { font-size: var(--font-sm); color: var(--text-secondary); }

.note-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: var(--spacing-md); }
.note-label { font-size: var(--font-md); color: var(--text-regular); }
.note-count { font-size: var(--font-sm); color: var(--text-secondary); }
.note-input { width: 100%; min-height: 140rpx; padding: var(--spacing-md); border: 2rpx solid var(--border-color); border-radius: var(--radius-md); font-size: var(--font-md); box-sizing: border-box; }

.footer { position: fixed; bottom: 0; left: 0; right: 0; background: var(--bg-card); padding: var(--spacing-lg) var(--spacing-md); padding-bottom: calc(var(--spacing-lg) + env(safe-area-inset-bottom)); box-shadow: 0 -4rpx 20rpx rgba(0,0,0,.06); }
.btn-confirm { width: 100%; background: var(--success-color); color: #fff; border: none; border-radius: var(--radius-round); font-size: var(--font-lg); font-weight: 700; padding: var(--spacing-lg) 0; }
.btn-confirm::after { border: none; }

.modal-overlay { position: fixed; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0,0,0,.5); display: flex; align-items: flex-end; z-index: 1000; }
.modal-content { width: 100%; background: var(--bg-card); border-radius: var(--radius-xl) var(--radius-xl) 0 0; padding: var(--spacing-xl) var(--spacing-lg) calc(var(--spacing-xl) + env(safe-area-inset-bottom)); max-height: 80vh; overflow-y: auto; }
.modal-title { display: block; font-size: var(--font-xl); font-weight: 700; color: var(--text-primary); text-align: center; margin-bottom: var(--spacing-sm); }
.modal-desc { display: block; font-size: var(--font-sm); color: var(--text-secondary); text-align: center; margin-bottom: var(--spacing-lg); }
.modal-list { margin-bottom: var(--spacing-lg); }
.modal-item { display: flex; align-items: center; padding: var(--spacing-md) 0; border-bottom: 2rpx solid #f5f5f5; }
.modal-item:last-child { border-bottom: none; }
.modal-item-disabled { opacity: 0.4; }
.modal-item-info { flex: 1; }
.modal-item-name { font-size: var(--font-md); color: var(--text-primary); display: block; }
.modal-item-desc { font-size: var(--font-sm); color: var(--text-secondary); display: block; margin-top: 4rpx; }
.modal-check { width: 44rpx; height: 44rpx; border-radius: 50%; background: var(--success-color); color: #fff; display: flex; align-items: center; justify-content: center; font-size: 28rpx; font-weight: 700; }
.modal-cancel { width: 100%; background: #f5f5f5; color: var(--text-primary); border: none; border-radius: var(--radius-round); font-size: var(--font-md); font-weight: 600; padding: var(--spacing-md) 0; }
.modal-cancel::after { border: none; }

/* ========== 楼层数（选填）+ 楼层凭证（v43） ========== */
.floor-row {
  display: flex;
  align-items: center;
  margin: 12rpx 0 8rpx;
}

.floor-input {
  width: 200rpx;
  padding: 12rpx 20rpx;
  background: #f5f6f8;
  border-radius: 10rpx;
  font-size: 30rpx;
  color: #1f2329;
}

.floor-unit {
  font-size: 26rpx;
  color: #8a9099;
  margin-left: 12rpx;
}