# 更新日志

本文件记录本项目的**能力变更**。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号将遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **本项目尚未发布正式版本。** 下面的 `Unreleased` 是 `master` 上当前已具备的能力，
> 涵盖 2026-07-22 至 2026-09-23 的开发成果。逐条改动明细见 `git log`；
> 每一条能力都有对应的集成测试，件数以 `AquaFlow-backend/build/test-results/test/*.xml` 为准。

## [Unreleased]

### Added

**领域模型**

- **三站语义**：订单区分归属站（`orders.station_id`，定价方）、履约站（`orders.delivery_station_id`，库存与配送员清单）、结算站（`orders.settle_station_id`，营收与应收归属）。取代早期「双站模型」，收口了跨站场景下"钱记谁、货走谁"的口径。
- **桶权益账本**：以 `customer_barrel_lot`（押金条批次）为金额唯一真相源，桶权益按 FIFO 批次核销；引入「权益 / 配送中 / 持有 / 占用」四个数与恒等式 `占用 = 权益 + over`；`customer_barrel_over` 允许为负（水站暂存），且按商品各算。
- **退桶与欠桶闸门**：欠着空桶不许退桶（硬拦两处：预览与退款各查一次），按商品分别判定。
- **首单口径**：区分「本站第一笔买桶订单」，首单跳过回桶核对并按满额收押金。
- **支付状态机**：`0 未付 / 1 待收款 / 2 已付 / 3 已退款 / 4 已取消`，只前进不倒滚；唯一写「已付」的入口是带 CAS 的 `markPaidIfCollectable`。
- **水票批次**：`ticket_lot` 为余额真相源，`ticket_account` 退化为派生汇总；消耗按 FIFO、退款按流水当时单价回补，任何改动水票数量的路径都必须过批次账。

**业务能力**

- **三种支付方式**：微信（渠道未接入）/ 现金（货到付款）/ 水票（扣票成功即视同已付）。现金单下单即进入待收款，客户未开通货到付款时下单被拒。
- **退款双入口，均原路径返回**：取消订单（退水票 → 退流水 → 退押金 → 清配送中桶 → 回补库存）与单笔退款（不取消订单）。无订单的在线购票退款当前明确拒绝。
- **配送计费**：起送量、配送范围（直线距离，无坐标必须放行）、运费、楼层费；费用各自成列，绝不并入水费或押金。规则实现唯一（`DeliveryFeeUtil`），报价与下单共用同一入口。
- **配送员计件工资**：站级单价 + 收益明细 + 工资结算单（草稿 → 已确认 → 已发放），按桶逐商品计件，收益钉在"送达"故免疫取消；工钱走独立对账等式，不进客户对账。站长可自定义加/扣项条目。
- **应收账款**：金额真相源仍是 `payment_status = 1 且未取消`，不新造口径；核销即视为已收款。**账期为站级**，同一客户在不同水站可分别设置月结或现结。
- **跨站履约**：定向外派（需接收站确认）、抢单池、指定退回。含押金或桶权益的单禁止进抢单池。**被接单后该单归接单站管**，归属站只能在未被接单前召回。
- **桶异常单闭环**：异常登记 → 站长处置 → 核销或撤回权益，含类别白名单。
- **站长资产调整单**：人工补录历史资产的唯一合法来源，单据头 + 反向单撤销 + 幂等键，并纳入对账。
- **商品目录**：通用商品库 + 站级定价覆盖 + 站长自定义商品（可上报平台）。
- **运营支撑**：营业状态（软状态，不阻断下单只提示）、本站公告、站长待办汇总、经营毛利报表（成本按结算站 join）、站长端信息架构重组。
- **客户检索**：站长端搜客户从强子串 LIKE 改为相关性匹配（SQL 只取候选集、打分在 Java 单点实现），支持缩写、中文数字与无标点输入。

**一致性与可观测性**

- **守恒对账**：覆盖押金、桶、水票、支付与计件工资的十条等式，每日落 `reconciliation_result` 表；等式设计上刻意排除"合法业务状态"，避免把正常经营算成差异。
- **分级告警**：系统故障与运营故障分流投递（`alert_log`），系统告警无 HTTP 入口。
- **幂等护栏**：支付流水的部分唯一键（用 STORED 生成列实现，避免与退款冲正流水冲突）、无订单购票的客户端幂等键、三张流水表的调整场景唯一键。

**安全**

- JWT 双 Token（access 30 分钟 / refresh 7 天）与 `@RequireRole` + `@RequireStation` 的 AOP 统一授权。
- 登录类端点按来源 IP 限流（超限 429）。
- 跨租户可见面收窄：外派字段只带钱货去向，客户画像由 `CustomerProfileMask` 单点抹除。
- 密钥全量转为环境变量，`application-local.yml` 与 `.env*` 一律不入库；CI 增加敏感信息扫描步骤。

**工程**

- 集成测试体系：真实 MySQL + 真实 HTTP，基类断言库名含 `test`，覆盖并发、对账、跨站隔离等场景。
- 静态门禁六项（wxml 事件绑定、页面可达性、require 层级、悬空注释、JS 解析期错误、场景矩阵一致性）接入 CI。
- 场景测试矩阵：按业务场景而非测试类组织的覆盖地图，由脚本校验其声称的测试类/方法真实存在。
- 数据库迁移脚本全部幂等（`information_schema` 预检 + `PREPARE`），迁移清单与执行证据登记在 `AquaFlow-backend/sql/README.md`。

### Changed

- **跨站语义收口**：订单归属从「双水站模型」演进为三站语义；营收、应收、收款与退款判权统一认**结算站**，而客户资产（押金 / 水票 / 桶权益）仍认**归属站**。
- **桶汇总口径统一**：任何按商品或桶型的桶汇总必须取「权益 ∪ 配送中 ∪ 过占」并集；还桶上限用「占用」而非「持有」。
- **退款判权**：从「认归属站」改为「认结算站」。
- **账期归属**：从客户级改为站级，同一客户可在不同水站有不同账期。
- **CAS 方法命名收口**：`updateStatusTo(id, 新, 期望)` 与 `updateStatusIf(id, 期望, 新)` 按名字区分参数顺序，消除同名反序导致的静默失败。

### Fixed

- **对账日结永远不平**：修正三处把合法业务状态算成差异的等式（核销只置状态不补流水、把无订单购票当孤儿、把配送中桶的合法终态当异常）；收款改为必须两步（先记流水再 CAS 置已付）。
- **微信测试号换不出 openid**：顾客端从测试号切换为正式 appid —— `wx.login` 的 code 只能用签发它的 appid + secret 换取。
- **首单仍在配送途中时商品整行消失**：桶汇总漏掉 `customer_barrel_asset` 之外的两张表。
- **结算单漏掉当天收益**：时间区间上界由 `<= 结束日` 修正为 `< 结束日 + 1 天`。
- **MySQL `SET` 从左到右求值**：批次状态判定依赖已被更新的列值，调整赋值顺序后修复。
- **站长端管理列表丢失草稿与已下架公告**：列表查询不再附带 `status = 1` 过滤。
- **已送达订单禁止取消**、取消权限口径统一。
- 若干权限与越权修正：人工调整的越权、解除员工的护栏、跨站召回外派单的时机。

### Security

- 建立凭据管理规则：密钥只走环境变量，仓库、文档、日志与回复中一律不回显；新增提交前密钥扫描。
- 修复登录端点仅按用户名锁定可被绕开的问题，补充按来源 IP 的正交限流层。
- 系统故障告警不投递给水站站长，避免跨租户越权知情。
