# 文档索引

> 本文件索引仓库内**面向读者**的文档。代码结构不要问文档，问代码本身。

## 事实基准

发生冲突时，以这个顺序为准（高 → 低）：

```
1. 源码               AquaFlow-backend/src/**、miniapp-*/**（含 wxml / js）
2. 表结构             AquaFlow-backend/sql/schema.sql
3. 通过的集成测试      与接口的实际行为
4. 根 AGENTS.md       工程契约（领域不变量、已知坑、禁改项）
5. docs/**            说明性文档（本目录）
```

**任何一条文档若与代码冲突，以代码为准，并回来改文档。**

## 架构

| 文档 | 内容 |
|---|---|
| [`architecture/01-系统架构.md`](./architecture/01-系统架构.md) | 系统概览、部署拓扑、后端分层、横切关注点、模块划分、外部集成、关键架构决策 |
| [`architecture/02-领域模型.md`](./architecture/02-领域模型.md) | 业务域划分、客户全局身份、**三站语义**、**桶权益模型**、三本账、状态机、术语约定、待确认事项 |
| [`architecture/03-数据模型.md`](./architecture/03-数据模型.md) | 50 张表的分组与职责、各账线的金额真相源、对账等式的设计约束 |

## 接口

| 文档 | 内容 |
|---|---|
| [`api/01-REST-API参考.md`](./api/01-REST-API参考.md) | 统一响应体与 `code` 语义、认证与三种登录入口、**公开路径白名单**、授权与站点归属校验、按业务域分组的完整端点表（268 个端点）、已删除端点清单 |

## 运维

| 文档 | 内容 |
|---|---|
| [`operations/01-部署与运维.md`](./operations/01-部署与运维.md) | 部署形态、环境变量三类分界、启动、数据库迁移执行流程、备份与恢复、对账作业与告警分级、常见故障处置、已知运维边界 |
| [`本地运行-笔记本当服务器.md`](./本地运行-笔记本当服务器.md) | 不买服务器与域名的本地联调做法：启动顺序、局域网真机预览、防火墙、公开选站与 appid 组合 |

## 开发

| 文档 | 内容 |
|---|---|
| [`development/01-测试体系.md`](./development/01-测试体系.md) | 为什么以集成测试为主、两个测试基类的 API、断言约定、静态门禁、CI 步骤、统计件数的陷阱 |
| [`development/02-数据库迁移.md`](./development/02-数据库迁移.md) | 迁移脚本的编号与命名、**幂等实现范式**（含 MySQL 不支持的 `IF NOT EXISTS` 写法与实测报错）、破坏性操作流程、基线同步要求 |
| [`../CONTRIBUTING.md`](../CONTRIBUTING.md) | 环境搭建、构建与运行、提交规范、代码约定、迁移约定、小程序约定 |
| [`../SECURITY.md`](../SECURITY.md) | 凭据管理规则、启动期强制校验、已实施的安全控制、部署检查清单、已知边界 |

## 验证与审计

这几份是**工程契约**而非说明文档：它们描述"哪些东西在什么条件下必须成立"，
会被 CI 或人工流程直接使用，因此与代码同步维护。

| 文档 | 内容 |
|---|---|
| [`audit/2026-09-16-场景测试矩阵.md`](./audit/2026-09-16-场景测试矩阵.md) | **按业务场景**（而非测试类）组织的覆盖地图：每个场景的已测 / 部分 / 未测状态与证据、空白清单。新增用例前先看它。**由 `audit_scenario_matrix.py` 在 CI 中校验** |
| [`audit/删除登记表.md`](./audit/删除登记表.md) | **删除登记表**：删除代码 / 端点 / 文件前的六项报备协议、核实纪律，以及已确认删除的端点清单 |
| [`audit/write-path-inventory.md`](./audit/write-path-inventory.md) | 各表的写入口清单。实现新的写路径前先查它，避免绕开唯一写入口 |
| [`audit/test-harness.md`](./audit/test-harness.md) | 集成测试快速上手（与 `development/01-测试体系.md` 互补：后者讲设计，本篇讲怎么跑） |
| [`../AGENTS.md`](../AGENTS.md) | **工程契约**：领域不变量、禁改项、已知坑与判据。面向在仓库里工作的 AI agent，也是本仓库最完整的一份工程约定 |

## 维护规则

本目录的文档曾经大面积过期，根因是**同一个数值被写在多处**，改代码时改不全。
因此立三条规则：

1. **数值不重述，指向正本。** 会漂移的计数（测试类数、用例数、表数、迁移号、页数…）
   只在唯一真相源处定义，其他文档写指针。

   | 想知道 | 真相源 |
   |---|---|
   | 表与字段 | `AquaFlow-backend/sql/schema.sql` |
   | 迁移顺序与执行证据 | `AquaFlow-backend/sql/README.md` |
   | 枚举取值 | `AquaFlow-backend/src/main/java/com/example/aquaflow/constant/*.java` |
   | 接口实路径 | `controller/**` 上的注解 |
   | 测试件数 | `build/test-results/test/*.xml` |
   | 仓库结构 | 直接看目录树 |

2. **代码变更时同步更新它的真相源**，其他文档通过引用获取该事实，不复制。
3. **文档过期按缺陷处理** —— 发现漂移就修，不做"顺手改一下"。

## 设计规格

`design/` 下的编号文档是各功能模块的设计规格与决策记录：商品与库存、营业状态与公告、商品图片库、
配送计费、配送员计件与工资、水票档位与批次、业务流程地图、站长端信息架构、客户检索口径、
范围决策与实施路线图，以及若干评估与提案（未实施的方案评估）。
每篇开头都有文档头（状态 / 适用对象 / 关联代码 / 关联迁移），**状态为「已实施」的才是当前行为**。

> 模块清单会随开发变化，**以 `design/` 目录实际内容为准**，本文不逐个罗列。

## 待补充

以下文档尚未编写，属于已知缺口：

| 计划 | 内容 |
|---|---|
| `design/` 的模块索引 | 模块清单会随开发变化，当前以 `design/` 目录实际内容为准；若条目继续增加，值得加一篇按模块组织的索引 |
